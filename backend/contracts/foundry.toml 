[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.13"

# Optimization settings - reduces deployment cost
optimizer = true
optimizer_runs = 200

# Via IR for better optimization
via_ir = false

[rpc_endpoints]
base_sepolia = "https://sepolia.base.org"
local = "http://127.0.0.1:8545"

[etherscan]
base_sepolia = { key = "${BASESCAN_API_KEY}" }